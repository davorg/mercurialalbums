#!/usr/bin/env perl
use strict;
use warnings;

use Getopt::Long qw(GetOptions);
use Mercurial::App;

my %opts;

GetOptions(
  \%opts,
  'db=s',
  'out=s',
) or die "Usage: $0 --db DB --out DIR\n";

my $app = Mercurial::App->new(%opts);

$app->run;
